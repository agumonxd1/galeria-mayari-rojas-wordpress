document.addEventListener('DOMContentLoaded',()=>{const b=document.querySelector('.gmr-menu-toggle'),n=document.querySelector('.gmr-nav'),h=document.querySelector('.gmr-header');if(b&&n){const close=()=>{n.classList.remove('is-open');b.setAttribute('aria-expanded','false');document.body.classList.remove('gmr-menu-open')};b.addEventListener('click',()=>{const open=!n.classList.contains('is-open');n.classList.toggle('is-open',open);b.setAttribute('aria-expanded',String(open));document.body.classList.toggle('gmr-menu-open',open)});n.addEventListener('click',e=>{if(e.target.closest('a'))close()});document.addEventListener('keydown',e=>{if(e.key==='Escape')close()})}if(h){const sync=()=>h.classList.toggle('is-scrolled',scrollY>24);sync();addEventListener('scroll',sync,{passive:true})}const slider=document.querySelector('[data-hero-slider]'),slides=slider?[...slider.querySelectorAll('.gmr-home-hero__slide')]:[];if(slides.length>1&&!matchMedia('(prefers-reduced-motion: reduce)').matches){let active=0,timer;const show=next=>{slides[active].classList.remove('is-active');slides[active].setAttribute('aria-hidden','true');active=next;slides[active].classList.add('is-active');slides[active].setAttribute('aria-hidden','false')},start=()=>{clearInterval(timer);timer=setInterval(()=>show((active+1)%slides.length),6500)};start();document.addEventListener('visibilitychange',()=>document.hidden?clearInterval(timer):start())}const items=document.querySelectorAll('.gmr-home-reveal');if(items.length){if(!('IntersectionObserver'in window)){items.forEach(el=>el.classList.add('is-visible'));return}const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}}),{rootMargin:'0px 0px -8% 0px',threshold:.08});items.forEach(el=>observer.observe(el))}});
