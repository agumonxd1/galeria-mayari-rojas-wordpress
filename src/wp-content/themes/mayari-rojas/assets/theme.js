document.addEventListener('DOMContentLoaded',()=>{const b=document.querySelector('.gmr-menu-toggle'),n=document.querySelector('.gmr-nav'),h=document.querySelector('.gmr-header');if(b&&n){const close=()=>{n.classList.remove('is-open');b.setAttribute('aria-expanded','false');document.body.classList.remove('gmr-menu-open')};b.addEventListener('click',()=>{const open=!n.classList.contains('is-open');n.classList.toggle('is-open',open);b.setAttribute('aria-expanded',String(open));document.body.classList.toggle('gmr-menu-open',open)});n.addEventListener('click',e=>{if(e.target.closest('a'))close()});document.addEventListener('keydown',e=>{if(e.key==='Escape')close()})}if(h){const sync=()=>h.classList.toggle('is-scrolled',scrollY>24);sync();addEventListener('scroll',sync,{passive:true})}const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches,slider=document.querySelector('[data-hero-slider]'),slides=slider?[...slider.querySelectorAll('.gmr-home-hero__slide')]:[];if(slides.length>1&&!reduced){let active=0,timer;const show=next=>{slides[active].classList.remove('is-active');slides[active].setAttribute('aria-hidden','true');active=next;slides[active].classList.add('is-active');slides[active].setAttribute('aria-hidden','false')},start=()=>{clearInterval(timer);timer=setInterval(()=>show((active+1)%slides.length),6500)};start();document.addEventListener('visibilitychange',()=>document.hidden?clearInterval(timer):start())}const rotator=document.querySelector('[data-artwork-rotator]'),groups=rotator?[...rotator.querySelectorAll('.gmr-home-bento')]:[];if(groups.length>1&&!reduced){let current=0;setInterval(()=>{groups[current].classList.remove('is-active');groups[current].setAttribute('aria-hidden','true');current=(current+1)%groups.length;groups[current].classList.add('is-active');groups[current].setAttribute('aria-hidden','false')},7500)}const voiceRotator=document.querySelector('[data-voice-rotator]'),voiceGroups=voiceRotator?[...voiceRotator.querySelectorAll('.gmr-home-voice-grid')]:[];if(voiceGroups.length>1){let previous=-1;try{previous=Number(sessionStorage.getItem('gmrVoiceGroup'))}catch(e){}const choices=voiceGroups.map((_,i)=>i).filter(i=>i!==previous),showVoice=next=>{voiceGroups.forEach((group,i)=>{const active=i===next;group.classList.toggle('is-active',active);group.setAttribute('aria-hidden',String(!active));group.querySelectorAll('a').forEach(link=>active?link.removeAttribute('tabindex'):link.setAttribute('tabindex','-1'))});try{sessionStorage.setItem('gmrVoiceGroup',String(next))}catch(e){}return next};let voiceCurrent=showVoice(choices[Math.floor(Math.random()*choices.length)]??0);if(!reduced)setInterval(()=>{voiceCurrent=showVoice((voiceCurrent+1)%voiceGroups.length)},8000)}const items=document.querySelectorAll('.gmr-home-reveal');if(items.length){if(!('IntersectionObserver'in window)){items.forEach(el=>el.classList.add('is-visible'));return}const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}}),{rootMargin:'0px 0px -8% 0px',threshold:.08});items.forEach(el=>observer.observe(el))}});
